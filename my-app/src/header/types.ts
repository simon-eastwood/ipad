
export type IHeaderProps = {
    /**
     * Defines id of the header component
     */
    //id?: string;
    /**
     * Defines the icon as ReactNode
     */
    //icon?: ReactNode;
    /**
     * Defines the src of the image
     */
    logo?: string;
    /**
     * Defines the menu className
     */
    //iconClass?: string;
    /**
     * Defines the brand name as string
     */
    appName?: string;


    /**
     * Defines the menu as ReactNode
     */
    //menuClass?: string;
    /**
     * Defines the left side of the (e.g. navigation)
     */
    leftSide?: React.ReactNode;
    /**
     * Defines the right side of the (e.g. search, action bar, alerts, user profile)
     */
    rightSide?: React.ReactNode;
    /**
     * Defines the background color of the header
     */
    backgroundColor?: string;
    /**
     * Children of the component
     */
    //children?: ReactNode;
};
